# Copy to inventory.ini (gitignored) and set the VM public IP from
# `terraform output public_ip`. Re-check after every stop/start (IP changes).
[measurement]
vm ansible_host=203.0.113.7 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_ed25519
